# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

source 'https://github.com/simplecomponent/MainProject.git'
source 'https://github.com/CocoaPods/Specs.git'

use_frameworks!

target 'MainProject' do
pod "HomePage_Extension", :path => "../HomePage_Extension"
pod "HomePage", :path => "../HomePage"

pod "native_wallet", :path => "../native_wallet"
pod "native_wallet_extension", :path => "../native_wallet_extension"
end
